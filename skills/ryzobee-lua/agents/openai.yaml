interface:
  display_name: "RyzoBee Lua"
  short_description: "依据固件源码编写 Lua 脚本，校验头部摘要、UI、外设接口与运行限制"
  default_prompt: "使用 $ryzobee-lua，依据当前固件接口完成脚本，并检查头部摘要、语法和相关行为。"
policy:
  allow_implicit_invocation: true
